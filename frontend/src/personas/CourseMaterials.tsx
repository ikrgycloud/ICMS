import { useEffect, useMemo, useState } from 'react'
import { api } from '../api'
import { Empty, Modal, Spinner } from '../modules/kit'
const pdf=(m:any)=>m.material_type==='pdf'||String(m.resource_url).toLowerCase().includes('.pdf')
export function FacultyMaterials(){const[d,setD]=useState<any>(null),[s,setS]=useState<any[]>([]),[q,setQ]=useState(''),[sec,setSec]=useState('all'),[show,setShow]=useState(false),[f,setF]=useState<any>({section_id:'',title:'',material_type:'pdf',resource_url:'',status:'published'});const load=()=>api.facultyMaterials().then(setD).catch(()=>setD({materials:[]}));useEffect(()=>{load();api.facultySections().then((x:any)=>setS(x.sections||[]))},[]);if(!d)return <Spinner/>;const all=d.materials||[],rows=all.filter((m:any)=>(sec==='all'||m.section_id===sec)&&(!q||`${m.title} ${m.course_code}`.toLowerCase().includes(q.toLowerCase()))),published=all.filter((m:any)=>m.status==='published').length;return <main className="assess-workspace fade-in"><section className="assess-heading"><h1>Course Materials</h1><p>Upload, organize, and share resources for your assigned courses and sections.</p></section><section className="assess-kpis">{[['▤',new Set(all.map((m:any)=>m.course_code)).size,'Assigned Courses'],['▧',all.length,'Total Materials'],['☁',published,'Published Resources'],['✎',all.length-published,'Draft Resources']].map(([i,v,l])=><article className="assess-kpi" key={String(l)}><i>{i}</i><div><b>{v}</b><small>{l}</small></div></article>)}</section><div className="assess-notice">ⓘ Materials are visible only to students enrolled in the selected section when published.</div><div className="assess-filters"><select value={sec} onChange={e=>setSec(e.target.value)}><option value="all">All Sections</option>{s.map((x:any)=><option value={x.id} key={x.id}>{x.course_code} · {x.section}</option>)}</select><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search materials..."/><button className="btn btn-crimson" onClick={()=>setShow(true)} type="button">＋ Upload Material</button></div><article className="assess-register"><header><h2>Material Library</h2></header><div className="assess-table-wrap"><table className="assess-table"><thead><tr><th>Course</th><th>Section</th><th>Title</th><th>Type</th><th>Uploaded On</th><th>Visibility</th><th>Actions</th></tr></thead><tbody>{rows.map((m:any)=><tr key={m.id}><td><b>{m.course_code}</b><span>{m.course_title}</span></td><td>{m.section}</td><td>{m.title}</td><td>{m.material_type}</td><td>{m.uploaded_at?new Date(m.uploaded_at).toLocaleDateString():'—'}</td><td><em>{m.status}</em></td><td>{pdf(m)?<a href={m.resource_url} target="_blank" rel="noreferrer">View PDF</a>:<a href={m.resource_url} target="_blank" rel="noreferrer">Open</a>}</td></tr>)}{!rows.length&&<tr><td colSpan={7}>No materials match the selected filters.</td></tr>}</tbody></table></div></article>{show&&<Modal title="Upload PDF Material" onClose={()=>setShow(false)} footer={<button className="btn btn-crimson" onClick={async()=>{await api.createFacultyMaterial(f);setShow(false);load()}} type="button">Save Material</button>}><div className="form-row"><label>Section</label><select className="select" value={f.section_id} onChange={e=>setF({...f,section_id:e.target.value})}><option value="">Select section</option>{s.map((x:any)=><option value={x.id} key={x.id}>{x.course_code} · {x.section}</option>)}</select></div><div className="form-row"><label>Title</label><input className="inp" value={f.title} onChange={e=>setF({...f,title:e.target.value})}/></div><div className="form-row"><label>PDF URL</label><input className="inp" value={f.resource_url} onChange={e=>setF({...f,resource_url:e.target.value})}/></div></Modal>}</main>}
export function StudentMaterials(){const[d,setD]=useState<any>(null);useEffect(()=>{api.studentMaterials().then(setD).catch(()=>setD({materials:[]}))},[]);if(!d)return <Spinner/>;return <main className="assess-workspace fade-in"><section className="assess-heading"><h1>Course Materials</h1><p>Published resources from your enrolled sections.</p></section>{d.materials.map((m:any)=><article className="card card-pad" key={m.id}><b>{m.title}</b><p>{m.course_code} · Section {m.section}</p><a href={m.resource_url} target="_blank" rel="noreferrer">{pdf(m)?'View PDF':'Open resource'}</a></article>)}{!d.materials.length&&<Empty text="No published materials are available."/>}</main>}
